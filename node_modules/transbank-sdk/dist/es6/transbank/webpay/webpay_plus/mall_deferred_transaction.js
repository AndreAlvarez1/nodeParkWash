import WebpayPlus from '.';
import RequestService from '../../common/request_service';
import MallTransaction from './mall_transaction';
import { MallCaptureRequest } from './requests';
const MallDeferredTransaction = Object.assign(Object.assign({}, MallTransaction), { capture: async (token, commerceCode, buyOrder, authorizationCode, amount, options = WebpayPlus.getDefaultOptions()) => {
        return RequestService.perform(new MallCaptureRequest(token, commerceCode, buyOrder, authorizationCode, amount), options);
    } });
export default MallDeferredTransaction;
